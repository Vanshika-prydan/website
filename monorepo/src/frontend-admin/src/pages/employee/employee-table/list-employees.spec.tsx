import React from 'react';
// import { render } from '@testing-library/react';
// import EmployeeListRow from './row';
// import { AccountModel } from '../../../models/account.model';

it('should renter the card', () => {
  // render(<EmployeeListRow employee={} />);
});
