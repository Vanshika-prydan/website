import { CircularProgress } from '@material-ui/core';
import React from 'react';

const InitialPageLoader: React.FunctionComponent = () => (
  <div>
    <CircularProgress />
  </div>
);

export default InitialPageLoader;
