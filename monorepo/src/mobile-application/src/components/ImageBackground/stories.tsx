import { storiesOf } from '@storybook/react-native';
import React from 'react';
import ImageBackground from '.';

storiesOf('ImageBackground', module).add('Default', () => (
  <ImageBackground></ImageBackground>
));
