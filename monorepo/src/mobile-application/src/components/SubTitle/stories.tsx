import React from 'react';
import { storiesOf } from '@storybook/react-native';
import SubTitle from '.';

storiesOf('SubTitle', module).add('default', () => (
  <SubTitle>Längd på städning</SubTitle>
));
