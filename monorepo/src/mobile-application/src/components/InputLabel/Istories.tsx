import React from 'react';
import { storiesOf } from '@storybook/react-native';
import InputLabel from '.';

storiesOf('Input/InputLabel', module).add('default', () => (
  <InputLabel>Längd på städning</InputLabel>
));
