import React from 'react';
import { storiesOf } from '@storybook/react-native';
import InputTitle from '.';

storiesOf('Input/InputTitle', module).add('default', () => (
  <InputTitle>Längd på städning</InputTitle>
));
